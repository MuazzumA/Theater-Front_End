import { FindTimesPipe } from './find-times.pipe';

describe('FindTimesPipe', () => {
  it('create an instance', () => {
    const pipe = new FindTimesPipe();
    expect(pipe).toBeTruthy();
  });
});
